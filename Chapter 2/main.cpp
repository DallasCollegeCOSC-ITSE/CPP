#include <iostream>

using namespace std;

int main (){
    //streams (both)
    //synchronous (blocking)
    //asynchronous (non-blocking)
    int mun = sqrt(39);
    cout << "How old are you" << endl;
    int age;
    cin << age;
    
}